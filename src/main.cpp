#include "core/Game.h"

// ===========================================================================
// 程序入口。
// 框架阶段：仅创建 Game 并启动主循环；具体实现待后续补全。
// ===========================================================================

int main() {
    Game game;
    if (game.Init()) {
        game.Run();
    }
    return 0;
}
