#include <core/GameContext.h>

// ===========================================================================
// GameContext：全局上下文，集中持有各管理器的裸指针。
// 所有成员已在头文件中通过 = nullptr 默认初始化，无需额外逻辑。
// Game 负责填充"共享"部分，PlayState 负责填充"局内"部分。
// ===========================================================================
